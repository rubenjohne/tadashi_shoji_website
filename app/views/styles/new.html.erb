<%= form_for(:style, :url => styles_path) do |f| %>
  <fieldset>
	<legend>New Style<legend>

		<%= render :partial => "form", :locals => {:f => f} %>
		
		<p>
			<%= submit_tag "Create" %>
		</p>
  </fieldset>
<% end %>