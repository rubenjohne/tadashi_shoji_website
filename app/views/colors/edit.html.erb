<h1>Editing Color</h1>

<%= form_for :color, :url => color_path(@color), :html => { :method => :put } do |f| %>

<fieldset>
	<legend>Edit Color</legend>
	
	<%= render :partial => 'form', :locals => {:f => f} %>
	
	<%= submit_tag "Update" %>
</fieldset>		
	
<% end %>	